## 코딩 컨벤션

### 접두사, 접미사 관련
- 씬 상에 컴포넌트로 부착해서 사용하는 경우를 구분해야 한다면 Runtime~ 접두사 사용
- MonoBehaviour 구현체가 아닌 요소에 ~Behaviour 접미사를 사용하지 않음
- MonoBehaviour 구현체와 순수 C# 클래스를 구분해야할 필요가 있을 때에는 ~Behaviour 및 ~Class 접미사로 구분함
